import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
          'home'), //tester to home page which prints home at the center of the page
    );
  }
}
